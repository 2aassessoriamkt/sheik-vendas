export const enhanceAdDescription = async (_title: string, currentDesc: string) => {
  return currentDesc;
};

export const suggestPrice = async (_title: string, _desc?: string) => {
  return null;
};
